# 고객 유입 트래킹 및 사은품 지급 관리 PRD (초안)

## 1. 프로젝트 개요

### 프로젝트명
삼화메디칼 전시회 고객 유입 및 사은품 관리 시스템

### 목표
오프라인 유입 고객의 디지털 자산화(DB화) 및 사은품 지급 프로세스의 데이터 검증을 수행한다.

### 성공 기준(정량)
- QR 스캔 시점에 `qr_logs`에 유입 기록이 누락 없이 저장된다.
- 등록 완료 고객이 `customers`에 저장되고, 고유 ID 기반 검증용 QR이 노출된다.
- 직원이 관리자 페이지에서 검증 QR을 스캔했을 때, 해당 고객의 `gift_status`와 `claimed_at`이 정확히 갱신된다.

## 2. 범위(Scope)

### 포함
- 배너 QR 스캔 -> 랜딩 페이지 진입 시 자동 로그 기록(`qr_logs`)
- 약관 동의/정보 입력 -> `customers` DB 저장
- 등록 완료 화면 -> 고객 고유 ID 포함 검증용 QR 노출
- 직원 검증 -> QR 스캔 후 '사은품 지급 완료' 처리(상태/일시 갱신)

### 제외(Out of Scope)
- 카카오 친구 추가 완료 여부의 자동 판정(수동 확인만 반영)
- 실제 카카오 API 연동을 통한 친구 추가 검증
- 사은품 재고/수량 관리(필요 시 별도 PRD)

## 3. 핵심 사용자 흐름(Workflow)

### 3.1 유입(Scan)
1. 배너의 QR에서 `source=XXX`를 포함해 랜딩 페이지로 진입한다.
2. 랜딩 진입 시 자동으로 스캔 이벤트가 기록된다.
   - 기록 대상: `qr_logs`에 `created_at`, `source` 저장

### 3.2 약관 동의
- 개인정보 활용 동의 및 마케팅 수신 동의를 수집한다.

### 3.3 정보 입력
- 성명, 연락처, 보유 제품, 관심 분야를 입력하고 `customers`에 저장한다.
- 입력 데이터는 이후 직원 검증 및 QR 생성에 사용된다.

### 3.4 카카오 친구 추가
- 카카오 채널 추가 버튼 클릭을 유도한다.
- 친구 추가 완료 여부는 **수동 확인**을 전제로 한다.

### 3.5 완료 및 QR 생성
- 등록 완료 화면에 해당 고객의 **고유 ID가 담긴 검증용 QR**을 노출한다.

### 3.6 직원 검증
- 직원이 관리자 페이지에서 고객의 QR을 스캔한다.
- '사은품 지급 완료'로 처리하여 `customers.gift_status` 및 `customers.claimed_at`을 갱신한다.

## 4. 데이터 요구사항(Supabase)

### 4.1 공통 원칙
- 스캔/등록/지급 단계별로 누락 없이 저장되어야 한다.
- 직원 검증은 고객 고유 ID를 기준으로 정확히 매칭되어야 한다.
- `claimed_at`은 지급 완료 시점에만 기록된다.

### 4.2 `qr_logs` 테이블
유입 스캔 기록을 저장한다.

| 컬럼 | 타입(권장) | 설명 |
|---|---|---|
| `id` | `uuid` (PK) | 로그 고유 식별자 |
| `created_at` | `timestamptz` | 스캔 시점 |
| `source` | `text` | 유입 경로(예: `QR_EVENT`, `QR_PRODUCT` 등) |

#### 저장 시점
- 랜딩 페이지 진입 시 자동 생성

#### 인덱스(권장)
- `created_at` 내림차순 조회가 잦을 경우 인덱스 추가
- `source` 필터링이 필요하면 `source` 인덱스 추가

### 4.3 `customers` 테이블
고객 정보를 저장하고 사은품 지급 상태를 관리한다.

| 컬럼 | 타입(권장) | 설명 |
|---|---|---|
| `id` | `uuid` (PK) | 고객 고유 ID (검증 QR에 포함) |
| `name` | `text` | 성명 |
| `phone` | `text` | 연락처 |
| `products_owned` | `text[]` 또는 `jsonb` | 보유 제품(요구사항: 단일/다중은 구현 결정) |
| `interests` | `text[]` | 관심분야 배열 |
| `source` | `text` | 유입 경로 |
| `gift_status` | `text` | 지급 상태 |
| `claimed_at` | `timestamptz` | 지급 일시(미지급이면 `NULL`) |

#### `gift_status` 값 정의(권장)
- `PENDING` : 등록 완료 후, 직원 검증 전
- `CLAIMED` : 직원 검증 완료(사은품 지급 완료)

#### 업데이트 규칙
- 직원 검증(관리자 페이지 QR 스캔) 시:
  - `gift_status` = `CLAIMED`
  - `claimed_at` = 현재 시각

## 5. 기능 요구사항(Functional Requirements)

### 5.1 랜딩 유입 기록
- 입력: 배너 QR의 `source=XXX`
- 동작: 랜딩 진입 시 `qr_logs`에 `(created_at, source)` 저장

### 5.2 약관 동의 및 정보 입력
- 동작: 고객이 성명/연락처/보유 제품/관심 분야를 입력하면 `customers`에 저장
- `source`: 유입 기록에 사용된 `source`를 고객에도 동일하게 저장

### 5.3 검증용 QR 생성/노출
- 동작: 등록 완료 화면에서 `customers.id`를 포함하는 QR을 생성하고 노출

### 5.4 직원 검증 처리
- 동작: 관리자 페이지에서 QR을 스캔하면 QR payload로 고객을 식별
- 처리: 고객의 `gift_status`와 `claimed_at` 갱신
- 검증 결과를 UI에 표시(성공/실패)

## 6. 사용자/화면 요구사항(UI/UX)

- 랜딩 화면은 이벤트(또는 배너)별로 동일한 흐름을 제공하되, 유입 `source`만 다르게 기록한다.
- 등록 완료 화면:
  - 고객 고유 ID가 포함된 검증 QR을 중앙에 배치
  - 직원이 스캔해야 한다는 안내 문구 포함
- 관리자 페이지:
  - 스캔 UI(카메라 입력 또는 QR 텍스트 입력) 제공
  - 스캔 결과에 따라 지급 완료 상태 표시

## 7. 검증(Validation) 요구사항

### QR/ID 매칭 검증
- 직원 검증 시 QR payload(고객 고유 ID)로 `customers` 레코드를 찾아야 한다.
- 해당 고객이 이미 `gift_status=CLAIMED`인 경우:
  - 상태가 변경되지 않도록 하고, 중복 처리 방지 로직이 필요하다(구체 정책은 구현 결정).

### 데이터 누락 검증
- `qr_logs` 저장이 실패하거나 `source`가 비어 있는 경우를 점검할 수 있어야 한다.
- `customers` 저장 시 필수 입력(성명/연락처 등)이 누락되면 저장을 막는다.

## 8. 운영/로그 요구사항(간단)
- 스캔 이벤트와 등록 이벤트는 최소한 `created_at` 기준으로 추적 가능해야 한다.
- 직원 검증 시 변경 이력(누가/언제/무엇을)까지는 필요 시 별도 테이블 또는 로그로 확장한다.

## 9. 오픈 이슈(추후 결정)
- `products_owned`가 단일 선택인지, 다중 선택인지(스키마 타입과 UI에 영향)
- QR payload에 보안(서명/토큰)이 필요한지(현재 요구사항엔 고객 ID만 명시)
- 관리자 페이지에서 스캔 오류(존재하지 않는 ID, 이미 지급 완료 등)의 정책

---

## 부록: QR 파라미터/페이로드 규격(권장 정리)
- 유입 랜딩 QR: `.../landing/{eventId}?source=XXX` 형태로 `source`를 전달
- 검증용 QR: `customers.id`(UUID 문자열)를 payload로 사용

